using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 明智： 提的策略，总是会以当前的取长补短
public class Wise : CharacterAffix
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
