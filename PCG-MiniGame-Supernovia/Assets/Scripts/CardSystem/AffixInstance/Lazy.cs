using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 懒惰： 有时候会不上早朝，不来献计
public class Lazy : CharacterAffix
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
